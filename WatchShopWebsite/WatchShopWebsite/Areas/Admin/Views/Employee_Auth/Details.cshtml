@model WatchShopWebsite.Models.EF.NhanVien_VaiTro

@{
    ViewBag.Title = "Chi Tiết Phân Quyền";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="shadow container mt-3 py-3">
        <div class="form-horizontal">
            <h2 class="text-center">Chi Tiết Phân Quyền</h2>
            <hr />
            @Html.HiddenFor(model => model.ID)

            <div class="row">
                <div class="form-group col-12 col-sm-12 col-md-6 col-lg-6">
                    <label class="form-label fw-bold">Tên Vai Trò</label>
                    <div class="col-md-12">
                        <input type="text" class="form-control bg-white" id="TenVaiTro" name="TenVaiTro" readonly value="@Model.VaiTro.TenVaiTro" />
                    </div>
                </div>
                <div class="form-group col-12 col-sm-12 col-md-6 col-lg-6">
                    <label class="form-label fw-bold">Tên Nhân Viên</label>
                    <div class="col-md-12">
                        <input type="text" class="form-control bg-white" id="TenNV" name="TenNV" readonly value="@Model.NhanVien.HoTen" />
                    </div>
                </div>
            </div>

            <div class="form-group mt-3">
                <label class="form-label fw-bold">Trạng Thái</label>
                <div class="col-md-12">
                    @{
                        var getStatus = Model.TrangThai == 0 ? "Ẩn" : "Hiển thị";
                    }
                    <input type="text" class="form-control bg-white" id="TrangThai" name="TrangThai" readonly value="@getStatus" />
                </div>
            </div>

            <div class="form-group text-center mt-3">
                @Html.ActionLink("Sửa", "Edit", new { id = Model.ID }, new { @class = "btn btn-sm btn-warning" })
                @Html.ActionLink("Quay Lại", "Index", null, htmlAttributes: new { @class = "btn btn-sm btn-secondary" })
            </div>
        </div>
    </div>
}